---
# _layouts/post.html
---
<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <title>{{ page.title }} | {{ site.title }}</title>
  <meta name="description" content="{% if page.description %}{{ page.description | strip_html | strip_newlines }}{% else %}{{ page.excerpt | strip_html | truncate:160 }}{% endif %}">
  <link rel="stylesheet" href="{{ '/assets/css/style.css' | relative_url }}">
  <!-- nếu anh có fontawesome id fa-css ở head, giữ nguyên -->
  {% if site.fa_css %}
  <link id="fa-css" rel="stylesheet" href="{{ site.fa_css }}">
  {% endif %}
  <script defer src="{{ '/assets/js/blog.js' | relative_url }}"></script>
</head>
<body class="{{ page.body_class | default: '' }}">
  <!-- topbar / header (giữ như trang chính nếu anh có include, hoặc minimal) -->
  <header class="topbar" role="banner">
    <div class="brand">
      <a href="{{ '/' | relative_url }}"><img src="{{ site.logo | default: '/assets/img/logo.png' }}" alt="{{ site.title }}"></a>
      <h3>{{ site.title }}</h3>
    </div>
  </header>

  <main class="container" role="main" style="padding-top:calc(var(--top-offset,84px) + 6px)">

    <!-- Breadcrumb (Trang chủ › Blog › Title) -->
    <nav aria-label="Breadcrumb" class="mapp-kicker" style="margin-bottom:12px; font-weight:700;">
      <a href="{{ '/' | relative_url }}">Trang chủ</a> ›
      <a href="{{ '/blog/' | relative_url }}">Blog</a> ›
      <span aria-current="page">{{ page.title }}</span>
    </nav>

    <!-- Hero (gồm title và 2 text links) -->
    <section class="hero-mini reveal" aria-label="Hero post">
      <div class="wrap">
        <div class="card">
          <div class="hmini-grid">
            <div>
              <h1 class="hmini-title">{{ page.title }}</h1>
              <p class="hmini-sub">
                {% if page.date %}Đăng ngày {{ page.date | date: "%-d %B %Y" }}{% endif %} • {{ site.title }}
              </p>

              <!-- two short text links dẫn tới menu -->
              <div style="margin-top:12px; display:flex; gap:10px; flex-wrap:wrap;">
                <a class="hmini-btn ghost" href="{{ '/chothuexemayhanoi/gioithieu.html' | relative_url }}"><i class="fa-solid fa-circle-info"></i> Giới thiệu</a>
                <a class="hmini-btn ghost" href="{{ '/chothuexemayhanoi/lienhe.html' | relative_url }}"><i class="fa-solid fa-phone"></i> Liên hệ</a>
              </div>
            </div>

            <div>
              <!-- mini card on right can be left empty or show meta -->
              <div class="hmini-tiles">
                <div class="grid">
                  <div class="hmini-tile">
                    <span class="hmini-icon"><i class="fa-solid fa-newspaper"></i></span>
                    <span>Blog</span>
                  </div>
                  <div class="hmini-tile">
                    <span class="hmini-icon"><i class="fa-solid fa-location-dot"></i></span>
                    <span>Hà Nội</span>
                  </div>
                </div>
                <div class="hmini-soft"></div>
                <div class="hmini-stats">
                  <div class="hmini-stat"><i class="fa-solid fa-calendar"></i>{{ page.date | date: "%Y" }}</div>
                  <div class="hmini-stat"><i class="fa-solid fa-user"></i>{{ page.author | default: site.author }}</div>
                  <div class="hmini-stat"><i class="fa-solid fa-tag"></i>{% for c in page.categories %}{{ c }}{% unless forloop.last %}, {% endunless %}{% endfor %}</div>
                </div>
              </div>
            </div>

          </div>
        </div>
      </div>
    </section>

    <!-- Post content -->
    <article class="mapp-card reveal" style="margin-top:18px;">
      <div class="mapp-head">
        <div>
          <h2 class="mapp-h1">{{ page.title }}</h2>
          <p class="mapp-kicker">Đăng: {{ page.date | date: "%-d %B %Y" }}</p>
        </div>
      </div>

      <div class="mapp-text">
        {{ content }}
      </div>

      <!-- optional: back to blog -->
      <p style="margin-top:18px;"><a href="{{ '/blog/' | relative_url }}" class="mapp-btn ghost"><i class="fa-solid fa-arrow-left"></i> Quay lại Blog</a></p>
    </article>

  </main>

  <!-- footer: giữ nguyên footer AI của site -->
  <footer class="premium app-footer" role="contentinfo">
    {% include footer.html %}
  </footer>

</body>
</html>
